
const shell=Arcade.setupShell({id:'circuit_lab',seed:1,emoji:'💡',title:'전기회로 구조대',subtitle:'도체를 골라 끊어진 회로를 이어 전구를 밝히세요.',goal:'도체와 부도체를 구분하고 전기 회로의 기본 조건을 설명할 수 있습니다.',mission:'전구 8개를 안전하게 밝히세요.',missions:['도체 5개 연속 선택','부도체 오답 2회 이하','전구 8개 밝히기','설명 힌트 읽고 정답 맞히기']});
const root=shell.els.root;root.innerHTML=`<div class="question-box"><b id="prompt">끊어진 회로에 넣을 물체를 고르세요.</b><p id="explain"></p></div><div class="choices" id="choices"></div><div class="question-box" id="circuit">🔋 ── ? ── 💡</div>`;
const conductors=['구리선','철 못','알루미늄 포일','동전','젖은 손수건'];const insulators=['고무 지우개','나무젓가락','플라스틱 자','마른 종이','유리컵'];let score=0,combo=0,level=1,time=100,lives=3,timer,answer;
function start(){shell.startAudio();score=0;combo=0;level=1;time=100;lives=YS_easyLives(3);newRound();clearInterval(timer);timer=setInterval(()=>{time--;shell.setHud({score,combo,level,time,lives});if(time<=0)finish();},1000);}shell.els.start.onclick=start;
function newRound(){answer=Arcade.choice(conductors);const opts=Arcade.shuffle([answer,...Arcade.shuffle(insulators).slice(0,3)]);document.querySelector('#explain').textContent='전기가 잘 통하는 물체를 도체라고 합니다.';const box=document.querySelector('#choices');box.innerHTML='';opts.forEach(o=>{const b=document.createElement('button');b.className='choice';b.textContent=o;b.onclick=()=>pick(o,b);box.appendChild(b);});document.querySelector('#circuit').textContent='🔋 ── ? ── 💡';}
function pick(o,b){if(o===answer){b.classList.add('correct');document.querySelector('#circuit').textContent=`🔋 ── ${o} ── 💡✨`;score+=110+combo*12;combo++;Arcade.sfx('good');if(combo%6===0){level++;Arcade.sfx('level');}setTimeout(newRound,700);}else{b.classList.add('wrong');document.querySelector('#circuit').textContent=`🔋 ── ${o} ── 💡…`;lives=YS_miss(lives);combo=0;Arcade.sfx('bad');if(lives<=0)finish();}shell.setHud({score,combo,level,time,lives});}
function finish(){clearInterval(timer);shell.end(score);}addEventListener('arcade-focus',e=>{time=Math.min(time,e.detail)});
